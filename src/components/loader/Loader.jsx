import React from 'react';
import PropTypes from 'prop-types';
import './loader.css';

/**
 * UI component for loader
 */
export const LoaderComponent = () => {
  return (
    <div className="storybook-loader"></div>
  );
};
